def build(path, ci_image) {
    stage("Build") {
        docker.image("egovio/ci-react:v8.4.0-lerna").inside {
            sh "cd ${path}/rainmaker && rm -rf node_modules &&  lerna bootstrap && cd packages/citizen && npm run build && cp -r build ../../../citizen/";
        }
    }
}

return this;
