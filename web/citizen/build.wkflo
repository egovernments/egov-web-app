def build(path, ci_image) {
    stage("Build") {
        docker.image("node:8.15.0-alpine").inside {
            sh "cd ${path}/rainmaker && yarn config set registry http://egov-micro-dev.egovernments.org/verdaccio && yarn run prod:citizen && cp -r build ../../../citizen/";
        }
    }
}

return this;
